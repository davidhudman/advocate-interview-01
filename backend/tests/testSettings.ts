/**
 * Shared settings for test configuration
 */
export const testSettings = {
  /**
   * When true, test data will be preserved in the database after tests run.
   * This can be useful for debugging or manual inspection of test results.
   * Set to false for clean tests and to avoid test data accumulation.
   */
  persistTestData: true,
};
