/**
 * Application-wide settings
 */
export const appSettings = {
  /**
   * When true, enhanced error logging will be enabled.
   * This provides more detailed error information in logs
   * but may expose sensitive information in production.
   */
  enhancedLogging: false,
};
